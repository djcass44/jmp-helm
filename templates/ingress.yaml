apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.name.api }}
  namespace: {{ .Values.namespace }}
  annotations:
   ingress.kubernetes.io/ssl-redirect: true
spec:
  rules:
  - host: {{ .Values.app.host }}
    http:
      paths:
        - path: /api 
          backend:
            serviceName: http
            servicePort: 7000
        - path: /socket.io
          backend:
            serviceName: ws
            servicePort: {{ .Values.port.ws }}
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.name.ui }}
  namespace: {{ .Values.namespace }}
  annotations:
   ingress.kubernetes.io/ssl-redirect: true
spec:
  rules:
  - host: {{ .Values.app.host }}
    http:
      paths:
        - path: /
          backend:
            serviceName: http
            servicePort: 80
