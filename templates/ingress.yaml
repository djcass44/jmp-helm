apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.name.api }}
  annotations:
   ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
  - host: {{ .Values.app.host }}
    http:
      paths:
        - path: /api 
          backend:
            serviceName: {{ .Values.name.api }}
            servicePort: {{ .Values.port.api }}
        - path: /socket.io
          backend:
            serviceName: {{ .Values.name.api }}
            servicePort: {{ .Values.port.ws }}
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: {{ .Values.name.ui }}
  annotations:
   ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
  - host: {{ .Values.app.host }}
    http:
      paths:
        - path: /
          backend:
            serviceName: {{ .Values.name.ui }}
            servicePort: 80
